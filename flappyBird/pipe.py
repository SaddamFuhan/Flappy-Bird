import pygame

	class Pipe:
		self.screen = MainFlappy.screen
		self.screen_rect = MainFlappy.screen.get_rect()
		self.game_setting = Mainflappy.game_settings
		self.image = self.screen_rect.bottomleft

		self.image_rect.bottomleft = self.screen_rect.bottomleft
		self.image_rect.x +=
		self.image_rect.y

	def apply_pipe(self):
		self.screen.blit(self.image, self.image_rect)